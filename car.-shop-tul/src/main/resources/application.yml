spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/car-shop-tul
    username: postgres
    password: root
  jpa:
    hibernate:
      show_sql: true
      format_sql: true
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect